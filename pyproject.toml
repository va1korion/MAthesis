[project]
name = "mathesis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "accelerate",
    "authlib>=1.5.2",
    "fastapi",
    "langchain",
    "langchain-chroma>=0.2.3",
    "transformers>=4.52.0",
]
